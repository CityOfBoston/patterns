{{#each links as |link|}}
  <a href="{{ link.href }}" title="{{ link.name }}" class="lwi g--4 {{../theme}}">
    <span class="lwi-ic"><img src="{{ link.image }}" alt="{{ link.name }}" class="lwi-i" /></span>
    <span class="lwi-t">{{ link.name }}</span>
  </a>
{{/each}}
