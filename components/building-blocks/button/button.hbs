<div class="b{{#if wrapper_theme}} b--{{wrapper_theme}}{{/if}}">
  {{#each buttons as |button|}}
    <div class="m-v400 m-h200">
      <{{type}} class="btn{{#each ../theme}} btn--{{this}}{{/each}}{{#if class}} {{class}}{{/if}}" {{#if disabled}}disabled{{/if}}>{{label}}</{{type}}>
    </div>
  {{/each}}
</div>
