{{#each textboxes as |textbox|}}
  <label class="txt">
    <span class="txt-l">{{label}}</span>
    <input type="{{#if type}}{{type}}{{else}}text{{/if}}" value="{{value}}" placeholder="{{placeholder}}" class="txt-f{{#if classes}} {{classes}}{{/if}}">
  </label>
{{/each}}
